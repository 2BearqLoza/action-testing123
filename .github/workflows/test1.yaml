name: test1

on:
  push:
    branches:
      - main
  workflow_dispatch:

#concurrency:
  #group: test-${{ github.head_ref }}

jobs:
  Init:
    runs-on: ubuntu-latest
    steps:
      - name: Initializing
      - uses: actions/checkou@2
        run: echo "Starting the test!"
        shell: bash
  
  # ReadFile:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Reading the ReadFile
  #     - uses: actions/checkou@2
  #       run: ubuntu-latest
